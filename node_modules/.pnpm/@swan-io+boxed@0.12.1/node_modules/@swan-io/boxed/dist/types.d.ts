export declare type LooseRecord<T> = Record<string | number | symbol, T>;
export declare type Concrete<T> = {
    [K in keyof T]-?: T[K];
};
export declare type Remap<T> = {
    [K in keyof T]: T[K];
};
