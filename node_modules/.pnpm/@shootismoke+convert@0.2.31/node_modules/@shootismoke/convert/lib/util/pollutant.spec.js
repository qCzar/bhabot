"use strict";
exports.__esModule = true;
var pollutant_1 = require("./pollutant");
describe('isPollutant', function () {
    it('should return true for pm25', function () {
        expect(pollutant_1.isPollutant('pm25')).toBe(true);
    });
    it('should return false for foo', function () {
        expect(pollutant_1.isPollutant('foo')).toBe(false);
    });
});
describe('getPollutantMeta', function () {
    it('should return correct metadata', function () {
        expect(pollutant_1.getPollutantMeta('pm25')).toEqual({
            id: 'pm25',
            name: 'PM25',
            description: 'Fine particulate matter (<2.5µm)',
            preferredUnit: pollutant_1.ugm3
        });
    });
});
