/**
 * Parts per billion.
 */
export declare const ppb = "ppb";
/**
 * Parts per million.
 */
export declare const ppm = "ppm";
/**
 * Microgram per cubic meter.
 */
export declare const ugm3 = "\u00B5g/m\u00B3";
/**
 * Array of pollutant concentration units.
 */
export declare const AllUnits: string[];
/**
 * Pollutant concentration units.
 */
export declare type Unit = typeof ppb | typeof ppm | typeof ugm3;
/**
 * All the pollutants tracked by @shootismoke.
 */
export declare type Pollutant = 'ch4' | 'co' | 'c6h6' | 'ox' | 'nh3' | 'nmhc' | 'no' | 'nox' | 'no2' | 'o3' | 'pm10' | 'pm25' | 'so2' | 'trs';
/**
 * Metadata for each pollutant.
 */
export interface PollutantMeta {
    id: Pollutant;
    name: string;
    description: string;
    preferredUnit: Unit;
}
/**
 * Array of all pollutants tracked by @shootismoke.
 */
export declare const AllPollutants: Pollutant[];
/**
 * Get metadata (code, name, description, unit) for a pollutant.
 *
 * @param pollutant - The pollutant to get the metadata from.
 */
export declare function getPollutantMeta(pollutant: Pollutant): PollutantMeta;
/**
 * Check if the input pollutant is a recognized pollutant which we can convert
 * AQI to/from raw concentrations.
 *
 * @param pollutant - The pollutant to test.
 */
export declare function isPollutant(pollutant: string): pollutant is Pollutant;
